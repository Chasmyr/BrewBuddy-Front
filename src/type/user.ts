export type UserInfo = {
    id: number,
    createdAt: string,
    currentMessageOrder: string,
    currentRecipe: string,
    email: string,
    pseudo: string,
    role: string,
    updatedAt: string
}